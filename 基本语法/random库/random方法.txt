random.choice(list) ：随机从列表中选取一个元素

random.randrange(start, end, step) ： 返回指定递增基数集合中的一个随机数，基数默认值为1。
start：指定范围的开始值，包含在范围内
end：指定范围的结束值，不包含在范围内
step：指定的递增基数

random.random()：随机产生[0,1)之间的数（浮点数）

random.shuffle(list)：将序列的所有元素随机排序

random.uniform(start, end)：随机生成一个实数，在[start, end]范围。

